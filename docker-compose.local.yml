services:
  app:
    build:
      context: .
      dockerfile: ./.config/docker/node/Dockerfile.local
    ports:
      - "3030:3030"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=local
